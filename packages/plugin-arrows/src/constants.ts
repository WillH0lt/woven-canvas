/**
 * Plugin name used for registration and resource lookup.
 */
export const PLUGIN_NAME = "arrows";

/**
 * Rank value for transform handles (should render on top).
 */
export const TRANSFORM_HANDLE_RANK = "zzzzz";

/**
 * Size of transform handles in pixels.
 */
export const TRANSFORM_HANDLE_SIZE = 24;

/**
 * Default padding for elbow arrow routing around blocks.
 */
export const ELBOW_ARROW_PADDING = 50;

/**
 * Default arrow thickness in pixels.
 */
export const DEFAULT_ARROW_THICKNESS = 4;

/**
 * Minimum pointer move distance to start dragging (in pixels).
 */
export const POINTING_THRESHOLD = 4;

/**
 * Maximum number of points in an elbow arrow path.
 */
export const ELBOW_ARROW_CAPACITY = 24;

/**
 * Size of arrow terminals (snap points) in pixels.
 */
export const TERMINAL_SIZE = 24;

/**
 * Distance in pixels at which arrow handles snap to terminals.
 */
export const TERMINAL_SNAP_DISTANCE = 40;
