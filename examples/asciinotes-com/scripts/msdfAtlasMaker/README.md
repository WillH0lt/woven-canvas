# msdfAtlasMaker

Generate a multi-channel signed distance field (MSDF) texture atlas from a set of font glyphs.

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.2.2. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
