---
title: Controlling the Canvas
description: Manipulate the contents of the canvas using the editor API.
---

import { Code } from '@astrojs/starlight/components';
import ExampleContainer from '../../../../components/ExampleContainer.astro';
import ControllingCanvasExample from './ControllingCanvasExample.vue';
import ControllingCanvasExampleRaw from './ControllingCanvasExample.vue?raw';

Manipulate the contents of the canvas using the editor API.

<ExampleContainer>
  <ControllingCanvasExample client:only="vue" />
</ExampleContainer>

<Code code={ControllingCanvasExampleRaw} lang="vue" title="ControllingCanvasExample.vue" />

You can manipulate blocks using the editor API. This example creates shapes on mount, cycles their fill colors while selected, and positions the camera. Access the editor via the `@ready` event or `useEditorContext()` from child components.
